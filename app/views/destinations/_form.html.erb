<%= form_for [@trip, @destination] do |f| %>
<%= f.hidden_field :trip_id, :value => @trip.id %>
  <%= f.label :country %><br>
  <%= f.text_field :country %><br><br>
  <%= f.label :state %><br>
  <%= f.text_field :state %><br><br>

  <%= f.submit "Create Destination" %>

<%end%>
